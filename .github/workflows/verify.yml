name: verify

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Install Playwright Chromium
        run: npm run test:browser:install
      - name: Verify project
        run: npm run verify
      - name: Run mock YouTube browser smoke test
        run: npx playwright test --config=playwright.config.ts --project=youtube-mock
        env:
          YTCQ_HEADLESS: '1'
