import { popupIconAnimationScenario } from '../../scenarios/popup-icons';
import { extensionScenarioTest as test } from '../../support/scenario-fixtures';

test('extension popup: bell motion stays inside the icon and respects reduced motion', popupIconAnimationScenario);
