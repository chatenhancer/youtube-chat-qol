---
import Icon from '../common/icon.astro';
import { site } from '../../data/site';
import type { Messages } from '../../data/site';

const { messages: m, rootPrefix } = Astro.props as {
  messages: Messages;
  rootPrefix: string;
};
---

<section class="playground-section" id="playground" aria-labelledby="playground-title">
  <div class="playground-inner">
    <div class="playground-copy">
      <div class="playground-heading-row">
        <span class="playground-section-icon" aria-hidden="true">
          <Icon name="playground" />
        </span>
        <span class="playground-eyebrow">{m.playground.eyebrow}</span>
        <span class="playground-beta-badge">Beta</span>
      </div>
      <h2 id="playground-title" set:html={m.playground.title}></h2>
      <p>{m.playground.copy}</p>
      <div class="playground-tags" aria-label={m.playground.tagsAria}>
        <span>{m.playground.tagOptIn}</span>
        <span>{m.playground.tagStream}</span>
        <span>{m.playground.tagChess}</span>
      </div>
      <div class="playground-actions">
        <a class="button button-primary" href="#install">{m.playground.primaryCta}</a>
        <a class="button playground-link" href={site.privacyUrl}>{m.playground.secondaryCta}</a>
      </div>
      <p class="playground-note">{m.playground.note}</p>
    </div>
    <figure class="playground-preview">
      <img src={`${rootPrefix}assets/playground/chess-preview.png`} alt={m.playground.imageAlt} loading="lazy" decoding="async">
    </figure>
  </div>
</section>
