# Enforce declared Node/npm engine requirements.
engine-strict=true

# Save newly added dependencies without automatic semver ranges.
save-exact=true

# Restrict Git, URL, and local-tarball dependency sources.
allow-git=none
allow-remote=none
allow-file=none

# Require an explicit decision about dependency install scripts.
strict-allow-scripts=true
